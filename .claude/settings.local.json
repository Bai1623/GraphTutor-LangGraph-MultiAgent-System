{
  "permissions": {
    "allow": [
      "Bash(npx next:*)",
      "Bash(npx tsc:*)",
      "Bash(xargs ls:*)",
      "Bash(git log:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && HASH=$(git log -1 --format=%h) && DATE=$(git log -1 --format=%as) && MSG=$(git log -1 --format=%s) && echo \"| $DATE | \\`$HASH\\` | $MSG |\" >> CHANGES.md.tmp && head -2 CHANGES.md > CHANGES.md.new && cat CHANGES.md.tmp >> CHANGES.md.new && tail -n +4 CHANGES.md >> CHANGES.md.new && mv CHANGES.md.new CHANGES.md && rm -f CHANGES.md.tmp"
          }
        ]
      }
    ]
  }
}